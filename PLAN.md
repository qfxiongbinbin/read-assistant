# readAssistant 规划

> 定位：**单语可读性改写**（monolingual text simplification）。不是把外语翻译成中文，而是把看不懂的外语降级成看得懂的**同一种外语**，让大脑直接建立 `外语形式 -> 含义` 的映射，不再经过中文中介。

## 一、四条硬设计原则

1. **零中文是硬约束**：注入页面的 UI 与模型输出双重约束，校验器检测 CJK 字符即判废。
2. **不替换原文**：简化版是对照层，改写失真时不会误导。
3. **只改难句，不全页改写**：成本、质量、学习效果的共同最优解。
4. **会毕业**：简化率应随水平**下降**，这是北极星指标——如果半年后还要天天点，产品就是失败的。

## 二、架构

```
[Chrome MV3 扩展]
  content script: 段落切分 / 标记 / 点击 / 就地渲染
  background:     缓存 -> DeepSeek -> 校验 -> 重试
        │
[词表数据] 5000 高频词（本地，用于难度判定，不耗 token）
```

关键点：**难度判定用本地词频表，不调模型**，模型只负责改写超纲内容。

## 三、决策记录

| 项 | 决策 | 日期 |
|---|---|---|
| 语言 | 仅英语 | 2026-09-14 |
| 模型 | DeepSeek，`deepseek-chat` 默认 | 2026-09-14 |
| 形态 | Chrome MV3，WXT + TypeScript | 2026-09-14 |
| 呈现 | 原文保留，点击段落看简化 | 2026-09-14 |
| 弹窗位置 | 三选一：`below` / `side` / `float`，默认 `below` | 2026-09-14 |
| 触发方式 | 点击段落（整段）+ 选中文字（句级） | 2026-09-14 |
| 结果抽取 | `keyWords`（单词）+ `keyPhrases`（短语/搭配），均为简单英文解释 | 2026-09-14 |
| 音标 | 本地词典（ipa-dict，MIT），英美双音标，零 token | 2026-09-14 |
| 朗读 | 浏览器 Web Speech API，en-GB / en-US | 2026-09-14 |

## 四、校验器规则（`src/lib/verify.ts`）

不合格则带上违规原因重试，最多 3 次。

| 规则 | 阈值 |
|---|---|
| 出现 CJK 字符 | 直接判废 |
| 平均句长 | <= 16 词 |
| 最长句 | <= 25 词 |
| 超纲词占比（去专有名词与术语） | <= 10% |
| 输出比输入长 | <= 2 倍 |
| 原文中的数字 | 必须全部保留 |
| 输出难度不得高于输入 | 允许 +5% 容差 |
| 抽取结果 | `keyWords` 至少 1 个；`keyPhrases` 可为空 |

词形还原用后缀剥离（`s/es/ed/ing/ly/er/est` + `ies->y` 等）近似。专有名词按"非句首的大写词"识别。

## 五、里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | prompt 与校验器验证 | 完成（selftest 20 例通过） |
| M1 | 扩展骨架：点击段落出简化 + 缓存 + popup | 完成 |
| M1.1 | 三种弹窗位置：段落底部 / 侧边 / 跟随光标 | 完成 |
| M1.2 | 句子级触发（选中即用）+ 关键词/短语抽取 | 完成 |
| M1.3 | 音标（英美双）与朗读（整段 / 单词 / 短语） | 完成 |
| M2 | 成本与体验：批量预取可视区、失败重试 UI、站点级排除 | 待开始 |
| M3 | 难度引擎：本地词频打分、自动标注超纲句、只改难句 | 待开始 |
| M4 | 个人画像：点击记录 -> 个人词表 -> 简化率曲线 | 待开始 |
| M5 | 本地服务化（Hono + SQLite）+ 多语言 | 待开始 |

## 六、风险与对策

| 风险 | 对策 |
|---|---|
| 改写失真误导 | 原文保留 + 校验器 + 可随时关闭面板 |
| 反而依赖简化版 | 默认对照而非替换；难度阈值后续自动升高 |
| 成本 / 延迟 | 段落缓存 + 只处理点击的段 + 校验重试上限 3 |
| API Key 泄露 | M1 只存 `chrome.storage.local`，不进仓库；M5 移到本地服务 |
| SPA / 动态页面 | MutationObserver + 600ms 防抖重扫，每页上限 600 段 |

## 七、明确推迟

账号体系、云同步、PDF/EPUB、Anki 导出、多语言、精细难度算法。
