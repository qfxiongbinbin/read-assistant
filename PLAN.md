# readAssistant 规划

> 定位：**单语可读性改写**（monolingual text simplification）。不是把外语翻译成中文，而是把看不懂的外语降级成看得懂的**同一种外语**，让大脑直接建立 `外语形式 -> 含义` 的映射，不再经过中文中介。

## 一、四条硬设计原则

1. **零中文是硬约束**：注入页面的 UI 与模型输出双重约束，校验器检测 CJK 字符即判废。划词解释（Explain）也遵守这条：它是"用最简单的外语重说"，不是译成中文。
2. **不替换原文**：简化版是对照层，改写失真时不会误导。
3. **只改难句，不全页改写**：成本、质量、学习效果的共同最优解。
4. **会毕业**：简化率应随水平**下降**，这是北极星指标——如果半年后还要天天点，产品就是失败的。

## 二、架构

```
[Chrome MV3 扩展]
  content script: 段落切分 / 标记 / 点击 / 选中 / 就地渲染
  background:     缓存 -> DeepSeek -> 校验 -> 重试
        │            ├─ simplify   句子/段落 -> CEFR 级别的简单英文 + 关键词抽取
        │            ├─ translate  词/短语   -> 最简单的词 + 一到三句简单句
        │            └─ explain    单词      -> 更简单的一句话解释（下钻）
        │
[词表数据] 5000 高频词（本地，用于难度判定，不耗 token）
```

关键点：**难度判定用本地词频表，不调模型**，模型只负责改写超纲内容。

## 三、决策记录

| 项 | 决策 | 日期 |
|---|---|---|
| 语言 | 仅英语 | 2026-09-14 |
| 模型 | DeepSeek，`deepseek-chat` 默认 | 2026-09-14 |
| 形态 | Chrome MV3，WXT + TypeScript | 2026-09-14 |
| 呈现 | 原文保留，段落悬停按钮触发简化 | 2026-09-18 |
| 弹窗位置 | 三选一：`below` / `side` / `float`，默认 `below` | 2026-09-14 |
| 触发方式 | 段落悬停按钮（整段）+ 选中文字；直接点击正文改为可选项且默认关闭 | 2026-09-18 |
| 结果抽取 | `keyWords`（单词）+ `keyPhrases`（短语/搭配），均为简单英文解释 | 2026-09-14 |
| 音标 | 本地词典（ipa-dict，MIT），英美双音标，零 token | 2026-09-14 |
| 朗读 | 浏览器 Web Speech API，en-GB / en-US | 2026-09-14 |
| 下钻解释 | 点任意词 → 用更简单的英文重新解释，可无限嵌套，面包屑回退 | 2026-09-14 |
| 划词解释 | `Explain` 与 `Simplify` 并存；**按选区长度分工**：词/短语只给 Explain，句子/段落两个都给；面板顶部可切 | 2026-09-18 |
| 划词门槛 | 从"≥20 字符且 ≥12 词"放宽到**任意非空选区**，否则单词划不出按钮 | 2026-09-18 |
| Explain 上限 | **≤ 40 词**（= 输出预算 3 句 × 14 词）。超出则不给这个动作：模型只能压缩，而校验器看不出信息被丢 | 2026-09-18 |
| 动作判定 | 抽成 `availableActions(text)`，胶囊、面板 tab、后端守卫**共用一处**，避免三处漂移 | 2026-09-18 |
| 划完即释 | popup 开关，默认关；只在 Explain 是选区唯一动作时生效，句子/段落仍点击触发 | 2026-09-18 |
| 零中文范围 | 保持不变：Explain 不引入中文，`SCHEMA_VERSION` 维持 `v2` | 2026-09-18 |
| P0 体验收敛 | Explain 命名、分层设置、API Key 测试、折叠学习项、上下文缓存 | 2026-09-18 |

## 四、校验器规则（`src/lib/verify.ts`）

不合格则带上违规原因重试，最多 3 次。

### Simplify（`verify`）

| 规则 | 阈值 |
|---|---|
| 出现 CJK 字符 | 直接判废 |
| 平均句长 | <= 16 词 |
| 最长句 | <= 25 词 |
| 超纲词占比（去专有名词与术语） | <= 10% |
| 输出比输入长 | <= 2 倍 |
| 原文中的数字 | 必须全部保留 |
| 输出难度不得高于输入 | 允许 +5% 容差 |
| 抽取结果 | `keyWords` 至少 1 个；`keyPhrases` 可为空 |

### Explain（`verifyTranslation`）

| 规则 | 阈值 |
|---|---|
| 出现 CJK 字符 | 直接判废（零中文不变） |
| 句数 | <= 3 |
| 平均句长 / 最长句 | <= 16 / <= 25 词 |
| 句中难词 | <= 1 个（原文专有名词豁免） |
| 输入 <= 6 词时复用输入的难词 | 直接判废 |
| 输出难度不得高于输入 | 允许 +5% 容差 |
| 数字 | 必须保留 |
| `term` 是否来自输入 | 必须（按词干宽松比对） |
| `simple` 是否真的更常见 | 必须，否则判废 |
| 输入有难词但 `plainWords` 为空 | 判废 |

**动作准入**不由校验器负责，而是在 `selection.ts` 的 `availableActions(text)` 里判一次，胶囊、面板 tab 和后端守卫共用：

| 动作 | 准入条件 |
|---|---|
| `Explain` | `词数 <= 40` 且 `字符数 <= 600`（后者是给没有词间空格的畸形输入兜底） |
| `Simplify` | `字符数 >= 20` 或 `词数 >= 12` |

词形还原用后缀剥离（`s/es/ed/ing/ly/er/est` + `ies->y` 等）近似。专有名词按"非句首的大写词"识别。

## 五、里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | prompt 与校验器验证 | 完成（selftest 20 例通过） |
| M1 | 扩展骨架：点击段落出简化 + 缓存 + popup | 完成 |
| M1.1 | 三种弹窗位置：段落底部 / 侧边 / 跟随光标 | 完成 |
| M1.2 | 句子级触发（选中即用）+ 关键词/短语抽取 | 完成 |
| M1.3 | 音标（英美双）与朗读（整段 / 单词 / 短语） | 完成 |
| M1.4 | 下钻解释：点词 → 更简单的解释 → 继续点（套娃） | 完成 |
| M1.5 | 划词解释：任意选区出胶囊、词/短语走 Explain、面板内切动作、划完即释 | 完成 |
| M1.6 | P0 体验：明确触发、popup 分层、连接测试、友好错误、信息折叠 | 完成 |
| M2 | 成本与体验：请求取消与去重、失败恢复、站点级偏好 | 待开始 |
| M3 | 难度引擎：本地词频打分、自动标注超纲句、只改难句 | 待开始 |
| M4 | 个人画像：点击记录 -> 个人词表 -> 简化率曲线 | 待开始 |
| M5 | 本地服务化（Hono + SQLite）+ 多语言 | 待开始 |

## 六、风险与对策

| 风险 | 对策 |
|---|---|
| 改写失真误导 | 原文保留 + 校验器 + 可随时关闭面板 |
| 反而依赖简化版 | 默认对照而非替换；难度阈值后续自动升高 |
| 成本 / 延迟 | 段落缓存（三套独立缓存）+ 只处理点击或选中的内容 + 校验重试上限 3 |
| 划词门槛放宽后误触发 | 默认点击触发；划完即释只在 Explain 是唯一动作时生效且默认关闭 |
| Explain 丢信息 | **结构性拦住**：输入 > 40 词就不给这个动作；prompt 同时要求"保留每一个要点"而不是"只保留主要意思" |
| API Key 泄露 | M1 只存 `chrome.storage.local`，不进仓库；M5 移到本地服务 |
| SPA / 动态页面 | MutationObserver + 600ms 防抖重扫，每页上限 600 段 |

## 七、明确推迟

账号体系、云同步、PDF/EPUB、Anki 导出、多语言、精细难度算法、中文翻译链路。
